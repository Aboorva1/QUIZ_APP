
<div class = "tables">
<table class="table table-striped table-bordered table-condensed">
<thead>
<tr>
  <th class="text-center">Option</th>
  <th class="text-center">Correct key</th>
  <th class="text-center">Actions</th>
</tr>
</thead>
<tbody>
  <% question_options.all.each do |option| %>
    <tr>
      <td><%= option.choice %></td>
      <td><%= option.is_correct_answer %></td>         
      <td> 
      <%= link_to "✎", edit_quiz_question_option_path(@quiz, @question, option), class: "btn btn-xs btn-primary" %> 
      <%= link_to 'Destroy', quiz_question_option_path(@quiz, @question, option), 
        method: :delete,
        data: { confirm: "Are you sure?" }, class: "btn btn-small btn-danger" %> 
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>

<p class="text-center">
<%= link_to 'Back', root_path, class: "btn btn-xs btn-info" %>
</p>